services:
  - type: web
    name: javed-portfolio
    runtime: node
    buildCommand: pnpm install && pnpm run build
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
